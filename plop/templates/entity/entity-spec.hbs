import {{class}} from '../../../../src/{{layer}}/entities/{{module}}/{{class}}';

describe('{{class}}', () => {
  test('equal', () => {
    const other: {{class}} = new {{class}}();
    other.prop = DEFAULT_PROP;
    expect(DEFAULT_{{upperCase (snakeCase class)}}.equal(other)).toBeTruthy();

    other.prop = null as any;
    expect(DEFAULT_{{upperCase (snakeCase class)}}.equal(other)).toBeFalsy();
  });

  test('clone', () => {
    const clone: {{class}} = DEFAULT_{{upperCase (snakeCase class)}}.clone();
    expect(clone.prop).toBe(DEFAULT_{{upperCase (snakeCase class)}}.prop);
  });

  test('toString', () => {
    expect(DEFAULT_{{upperCase (snakeCase class)}}.toString()).toBe([
      DEFAULT_{{upperCase (snakeCase class)}}.prop,
    ].join(','));
  });
});
