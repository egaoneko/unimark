import {{class}} from '../../../../src/{{layer}}/repositories/{{module}}/{{class}}';

describe('{{class}}', () => {
  const repository: {{class}} = new {{class}}();

  beforeEach(() => {
  });

  test('method', async () => {
    // expect().toBeTruthy();
  });
});
