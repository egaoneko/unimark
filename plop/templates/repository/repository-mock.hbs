import {
  Observable,
  of
} from 'rxjs';
// --ADD_IMPORT--

// --ADD_METHOD--

const mock{{class}} = jest.fn().mockImplementation(() => {
  return {
    // --APPLY_METHOD--
  };
});

export default mock{{class}};